"""Shipped OSS example strategies.

These are working, validated-on-synthetic-data strategies that new users
can backtest immediately after `rift init`. They are NOT the validated
private strategies the maintainers run on real capital — they're well-known
baseline approaches (momentum, mean reversion, funding arbitrage) included
as runnable starting points.
"""
