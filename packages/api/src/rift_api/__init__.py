"""RIFT API — HTTP REST server.

Lightweight wrapper around state files; no business logic. Read-only by
default. For dashboards, PMS integrations, and monitoring systems.

For AI agent access via MCP, run `rift serve` (CLI command — starts a local
stdio MCP server exposing 60+ tools that wrap the same engine the CLI uses).
"""
